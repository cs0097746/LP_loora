# V4 release gate

This marker exists to trigger the final CI run after the high-resolution V4 product assets were installed and the temporary transfer workflow was removed.

Release remains blocked until tests, lint, build, Playwright, and section-level visual QA all pass.
